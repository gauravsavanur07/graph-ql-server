type Query {
  info: String!
  feed: [Link!]!
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID! @unique
  createdAt:DateTime!
  description: String!
  url: String!
}
